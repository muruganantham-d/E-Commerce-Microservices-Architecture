# Demo script endpoints
AUTH_BASE_URL=http://localhost:4001
PRODUCT_BASE_URL=http://localhost:4002
ORDER_BASE_URL=http://localhost:4003

# Host-side Kafka access (services in Docker still use kafka:9092)
KAFKA_BROKERS=localhost:29092
REDIS_URL=redis://localhost:6379

# Service defaults (used when running a service outside Docker)
SERVICE_NAME=service-name
HTTP_PORT=4000
GRPC_PORT=50051
MONGO_URI=mongodb://localhost:27017/service_db
PRODUCT_GRPC_ADDR=localhost:50051
ORDER_CREATED_CONSUMER_GROUP=product-service-order-created-v1
ORDER_CREATED_CONSUMER_RETRY_MAX=3
ORDER_CREATED_CONSUMER_RETRY_INITIAL_DELAY_MS=250
ORDER_CREATED_CONSUMER_RETRY_MAX_DELAY_MS=2000
ORDER_CREATED_CONSUMER_RETRY_BACKOFF_MULTIPLIER=2
ORDER_IDEMPOTENCY_TTL_SECONDS=300
